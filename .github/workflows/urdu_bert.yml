name: BERT Urdu Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *' # every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HF Space
        run: |
          curl -X POST "https://oldahmed-bert-urdu.hf.space/predict" \
          -H "Content-Type: application/json" \
          -d '{"text": "keep alive"}'
