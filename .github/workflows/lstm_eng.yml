name: LSTM English Only TEST

on:
  schedule:
    - cron: '*/5 * * * *' # every 5 minutes
  workflow_dispatch:


jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping HF Space
        run: |
          curl -X POST "https://huggingface.co/spaces/oldAhmed/Docker_host" \
          -H "Content-Type: application/json" \
          -d '{"text": "keep alive"}'
